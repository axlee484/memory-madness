{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Play Scene",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "godot-mono",
            "args": [
                "--path", ".",
                "--scene", "res://${relativeFile}"
            ],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
        }
    ]
}